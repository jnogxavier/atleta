services:
  db:
    image: postgres:16-alpine
    container_name: atleta_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: atleta_development
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./tmp/db:/tmp/db
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
